export class Project {
  constructor (public name: string, public authors: string, public description: string, public askingAmount: number, public raisedAmount: number, public plans: string, public swag: number, public category: string, public favorited: number, public featured: number, public image: string, public authorImage: string) { }
}
